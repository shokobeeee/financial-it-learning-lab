(function(){
'use strict';
const S=window.CLOUD_SPEC;if(!S||!['aws','gcp','azure'].includes(S.id))return;
const phases=[['01–07','まず仕組みをつくる','Web導線 → Compute → Data → Network → Subnet → Route → Entry'],['08–15','安全に・止まりにくくする','Firewall → HA → Storage → Managed DB → IAM → Secret/Key → Observe → Backup'],['16–19','金融システムとして運用','Region/DR → Hybrid → Common Concept Translation → Change/Governance'],['20','War Room','Impact → Evidence → Safe Recovery → Financial Reconciliation']];
function patch(){const o=document.getElementById('modulePackageOverlay');if(!o||o.dataset.providerAligned==='1')return;o.dataset.providerAligned='1';const p=o.querySelector('.pkg-phases');if(p)p.innerHTML=phases.map(x=>'<div class="pkg-phase"><div class="pkg-range">'+x[0]+'</div><div><b>'+x[1]+'</b><span>'+x[2]+'</span></div></div>').join('');const grid=o.querySelector('.pkg-grid');if(!grid)return;const card=document.createElement('div');card.className='pkg-card';card.innerHTML='<h3>🔁 Cloud Fundamentalsと同じLab番号</h3><p>この教材は新しい20概念ではありません。Cloud Fundamentalsと同じ順番で、共通概念を <b>'+S.title+'</b> のサービス名へ翻訳します。迷ったら同じLab番号のCloud Fundamentalsへ戻ってください。</p><div class="pkg-next"><a class="primary" href="../cloud/">☁️ Cloud Fundamentalsへ戻る</a></div>';grid.insertBefore(card,grid.firstChild);if(Array.isArray(S.providerExtras)&&S.providerExtras.length){const extra=document.createElement('div');extra.className='pkg-card';extra.innerHTML='<h3>🧩 このProviderで追加で知っておくもの</h3>'+S.providerExtras.map(x=>'<div class="pkg-question"><b>'+x[0]+' — '+x[1]+'</b><br>'+x[2]+'</div>').join('')+'<p style="margin-top:9px">20 Labsの主導線を崩さないため独立Labにはしていません。必要な案件では Context / 公式資料で深掘りします。</p>';grid.appendChild(extra)}}
patch();new MutationObserver(patch).observe(document.body,{subtree:true,childList:true});
})();